import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    ESGContainer: {
        marginTop: 150,
        marginBottom: 200,
    },
    containerPage: {
        flex: 1,
    },
    containerModel: {
        flex: 1,
    },

});

export default styles;